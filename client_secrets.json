{
  "web": {
    "issuer": "${OIDC_ISSUER}",
    "auth_uri": "${OIDC_AUTH_URI}",
    "client_id": "${OIDC_CLIENT_ID_PACIENTES}",
    "client_secret": "${OIDC_CLIENT_SECRET_PACIENTES}",
    "redirect_uris": ["${OIDC_REDIRECT_URIS_PACIENTES}"],
    "userinfo_uri": "${OIDC_USERINFO_URI}",
    "token_uri": "${OIDC_TOKEN_URI}",
    "token_introspection_uri": "${OIDC_INTROSPECTION_URI}"
  }
}
